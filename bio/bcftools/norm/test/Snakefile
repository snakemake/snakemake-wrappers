rule norm_vcf:
    input:
        "{prefix}.bcf",
    output:
        "{prefix}.norm.vcf",
    log:
        "{prefix}.norm.log",
    params:
        extra="",  # optional parameters for bcftools norm (except -o)
    wrapper:
        "master/bio/bcftools/norm"
