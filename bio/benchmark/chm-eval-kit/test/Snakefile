rule chm_eval_kit:
    output:
        directory("resources/chm-eval-kit"),
    params:
        tag="v0.5", # Tag and version must match, see https://github.com/lh3/CHM-eval/releases.
        version="20180222",
    log:
        "logs/chm-eval-kit.log",
    cache: True
    wrapper:
        "master/bio/benchmark/chm-eval-kit"
