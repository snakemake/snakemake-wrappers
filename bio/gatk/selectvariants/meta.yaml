name: gatk SelectVariants
url: https://gatk.broadinstitute.org/hc/en-us/articles/360036362532-SelectVariants
description: |
  Run gatk SelectVariants.
authors:
  - Johannes Köster
  - Jake VanCampen
  - Filipe G. Vieira
input:
  - vcf: Path to vcf file
  - ref: Path to reference genome
output:
  - vcf: Path to filtered vcf file
params:
  - extra: The `extra` param allows for additional program arguments.
  - java_opts: The `java_opts` param allows for additional arguments to be passed to the java compiler, e.g. "-Xmx4G" for one, and "-Xmx4G -XX:ParallelGCThreads=10" for two options.
