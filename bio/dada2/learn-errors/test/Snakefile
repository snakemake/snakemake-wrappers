rule dada2_learn_errors:
    input:
    # Quality filtered and trimmed forward FASTQ files (potentially compressed) 
        expand("filtered/{sample}.1.fastq", sample=["a","b"])
    output:
        model="results/dada2/model_1.RDS",# save the error model
        plot="reports/dada2/errors_1.png",# plot observed and estimated rates
    log:
        "logs/dada2/learn-errors/learn-errors_1.log"
    threads: 1 # set desired number of threads here
    wrapper:
        "master/bio/dada2/learn-errors"
