rule samtools_sort:
    input:
        "mapped/{sample}.bam",
    output:
        "mapped/{sample}.sorted.bam",
    log:
        "logs/{sample}.log",
    params:
        mem="10G",
        extra="",
    threads: 8
    wrapper:
        "master/bio/samtools/sort"
