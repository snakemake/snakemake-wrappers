name: calc_consensus_reads
description: |
    Performs consensus read calculation on a marked bam records and merges single, 
    paired and skipped-consensus reads into a single bam file.
author: Felix Mölder